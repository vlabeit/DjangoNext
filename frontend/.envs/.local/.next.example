# Next.js (local Docker / compose)
# ------------------------------------------------------------------------------
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1

# Browser calls — the host machine reaches the Django published port.
# docker-compose.local.yml publishes Django on host port 8000.
NEXT_PUBLIC_API_URL=http://localhost:8000
